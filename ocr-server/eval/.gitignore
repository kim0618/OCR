# LEARN-LOOP-INFRA eval harness.
# Everything under eval/ is tracked (work locally -> push -> run on AWS -> pull back -> analyze).
# Only Python bytecode caches are excluded (machine-specific, regenerated, cause cross-machine noise).
__pycache__/
*.pyc
