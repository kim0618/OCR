---
name: project_invoice_item_table_p1_gate
description: run063 품목표 병목 전수조사 결과 + P1 진단 역전(free release 게이트가 좋은 출력 버림)
metadata: 
  node_type: memory
  type: project
  originSessionId: 5ccf9062-7163-4183-9557-11f591e33402
---

2026-07-07 run063(thin 2002장) 품목표 병목 전수조사(compare/*.json 직접 재집계). **base 재현: cell 37.892%, field 54.216%, master_or_code 행 49.95%, itemName 24.6/itemNameMaster 50.0/itemCode 39.5%.** 062 replay≈063(파서동일). ★062 metrics.json cell=13.37%는 master 적용前(Base)이라 비교금지—반드시 062 replay_compare(37.88%)와 비교.

**30pp 격차 부류분해(전부 실측):**
- 정렬/채점 주범 아님: alignment-noise 300 cells=+0.10pp(26파일 집중). → P5
- itemName 빈칸 3782 중 직접회수 695=+0.60pp뿐(72%는 gtOnly=행자체없음, P1종속). → P3 작음
- master 매칭로직 아님: 품명 match면 88% 성공. 순수결함 373행(+0.32pp), 75%는 (병)/(제약사품절) 접미사 near-miss. → P2 작음
- **itemCode 재분류: fine-tune 몫 아님.** itemCode match⊂itemNameMaster match(완전 하류). "98.7% recognition"=GT코드가 송장에 없는 내부코드라 OCR에 없음 → master-dict 코드체계 매핑 문제. → P4 오분류였음
- ext_missing 49634(43%)의 53%(26418)가 gtOnly(만들지못한 행). 파서_drop 상한 48788은 상한일뿐(46%가 unmade행에 갇힘)

**★P1 진단 역전(핵심):** fallback=전체행 71%(8904), moc 46.5% vs free 58.6%. 숫자열만 -22~30pp(quantity/itemCode/unitPrice/amount), itemName/spec은 동일 → 이미지난이도 아닌 열매핑 시그니처. **1411 fallback 전수 free replay: 1095장(78%)이 free가 올바른 bbox 열분리표를 만들었는데 free의 release 품질게이트(unitPrice/quantity_parseable_ratio, release_ready_ratio)가 탈락시켜 fallback text-order(="첫숫자前=품명" 가정, [_item_dict_from_row_text](invoice_statement.py:803))가 쓰레기(예 품명칸에 "30정")로 대체.** 실증 470711: free=제이프록센정/5/15,870(정답)인데 게이트탈락→fallback 30정. P1-B=316장(22%)만 free 진짜 miss.

**★P1-5 오프라인 측정으로 Direction A 기각(2026-07-07):** fallback 1411장에 free후보+master fill을 동일 채점(compare_table)한 결과 cell 33.8→11.2%(-22.7pp), 전 컬럼 하락. 이유=①free가 하드파일서 행 절반도 못만듦(후보4142 vs GT8904), ②free는 5컬럼모델이라 expiryDate/manufacturingNo/insuranceCode 0%, ③free itemName이 이 파일들선 8.4%(fallback23.8%)—게이트는 옳게 판단중. **470711 등 4샘플은 비대표 체리픽.** step6 "free58.6 vs fallback46.5"는 거의 전부 선택편향(free 강제시 11%)—free는 더 나은 파서 아니라 쉬운파일만 받음. 게이트=[invoice_statement_free.py:4883](invoice_statement_free.py), 분기=[main.py:3474](main.py). **남은옵션: P1-C(fallback 자체 bbox 열앵커, step6 853행 타깃)·P1-E(853행 mis-map vs OCR 분류 먼저). 큰 파서작업 전 853행 성격 측정.** 측정법=snapshot replay+compare_table 오프라인.

**★매칭트랙 2패치 완료(2026-07-07, 063 replay 검증):** 목표=구글 품명 **81.2%(learndata無 Master, BASELINE_MATRIX)**. 랭킹 전수분해(name-present master-fail 2444행): 클린/trigram탈락 779 + floor미달 830 + 오선택(랭킹) 810 + 사전없음 0. name-absent 3777행은 매칭 불가(행조립 몫) — 매칭 3레버 이론천장 69.6%, 81.2엔 행조립 필수. **패치1 clean_query_name**(fill_master_match 진입점, 선두 바코드/코드/행번호+말미 //n 스트립): master +198/reg 2. **패치2 V4c 랭킹**(match() 키 sim→dose→괄호접미사힌트 _sfx_score(부분포함 양방향)→price→잉여괄호·길이 + _DOSE '밀리그램/미리그램' 별칭 — 형제SKU (병)/(PTP)는 clean명 동일해 sim/dose 동률, V3은 price가 임의로 깸): master +183/reg 24. **누적: master 6221→6602(50.0→53.1%), code 4924→5226(42.0%), moc 49.95→53.01%(+3.06pp), cell 37.892→38.484%, spurious 0, study 무변화, 타컬럼 Δ0.** 루프: 오프라인 격리시뮬(run 재현오차 0 확인 후 격리)→패치→스모크→사용자 replay_compare 검증. **패치3 V4d 가격구제**(match(): floor 미달시 미달구간 키최선 후보가 단가↔bp1 1%이내+sim>=0.05면 배정, 루즈변형 기각): +117/회귀0/오배정15(88.6%). **패치4 품명입양 adopt_missing_item_names**(invoice_statement_free.py + main.py·replay_compare.py 합류점, fill_master_match 직전): itemName 빈 행에 amount/unitPrice 숫자 y-앵커(밴드=높이×1.2, 최소14px)로 미소비 품명전용 라인 입양. strict 가드(약품형태소 필수+회사/요약/4+자리숫자 배제)가 정밀도·회수 둘다 우위(92.3% vs 느슨79%). **★입양이 행조립 효과까지: 이름 생긴 잉여행 493개가 gtOnly와 신규정렬(gtOnly 2707→2214)→행당 ~7셀 동반회복. 4패치 누적(063 replay 07-08 검증): master 6221→7937(50.0→63.8%), code→6205(49.8%), itemName→4000(32.1%), moc 49.95→63.73%(+13.78pp), cell 37.892→42.034%(+4.14pp), spurious 0, study 무변화.** **패치5 행신설 synthesize_missing_rows**(invoice_statement_free.py + main.py·replay 합류점, 입양 뒤·master fill 앞): 미소비 품명라인+같은 y밴드 미소비 콤마금액 라인 쌍→(itemName,amount) 행 신설, 헤더불요. 3중게이트(스윕 확정)=①master사전 sim>=0.35 ②기존품명 fuzzy0.8 중복배제 ③콤마금액만. 무게이트=정크6269 기각→게이트후 신설844/GT정렬485/정크+359. **5패치 누적(063 replay 07-08 10:03 검증): master 6221→8348(50.0→67.1%), code→6494(52.1%), itemName→4382(35.2%), moc 49.95→67.03%(+17.08pp), cell 37.892→42.978%(+5.09pp), spurious 0, study 무변화.** 잔여 gap 81.2까지 14.1pp.

**★신설/입양 확장 소진 확정(2026-07-08 전수시뮬 4종 전부 기각/한계):** gtOnly 잔여 1764(read 1228) 게이트진단 → 중복이름 619·라인못찾음 547·embedded 245·형태소없음 230·콤마금액없음 231·sim미달 201. 확장시도: ①형태소보강(캡술 등)=+6 무의미 ②exact중복허용=정크+1802/정렬9% 기각(자기 소스라인 재신설) ③멀티셋소비(라인수-행수 예산)=+51/정크34(+0.10pp) 한계 ④한줄행신설=+1(기존 오추출행이 그 라인 출신이라 중복가드) — **룰 기반 행신설은 여기가 천장**. 중복행 619는 파서 내부 라인-소비 연계 필요(중규모 파서작업).

**★war 심층대조(2026-07-08, "룰 빠뜨린것" 검증):** war 품명 캐스케이드=clean→learndata→LIKE→trigram+price([master_match_google.sql](eval/data/invoice_war/master_match_google.sql)). 대조결과: ①우리 trigram 이미 clean-to-clean(_index=clean 마스터, query도 clean)=war 등가, 갭없음 ②LIKE=dose랭크로 재측정해도 net+9뿐(war -0.05pp·우리 기각 옳음) ③learndata=키 구글OCR이라 못씀+81.2 타깃서 제외 ④master_dict=war master 동일필터(use_yn=Y). **war엔 미채택 갭 없음.** BUT 우리 자체 [eval/item_name_clean.py](eval/item_name_clean.py) 'strip'(psql 7416 실측 승자 top1+2.3pp)이 **런타임에 부분만 배선**됨—오늘 clean_query_name은 선두만, strip은 중간·끝 junk(날짜/lot/금액/코드)까지. **패치6b: clean_query_name 완전판 교체**(중간junk제거+용량보호+O→0+PTP, 괄호구조 보존)=master +96/회귀27(용량토큰 보호로 dose신호 유지). 미커밋.

**패치6 HA중복행**(append_missing_ha_rows 가드①: 이름중복이어도 amount고유+수량×단가=금액 성립시 허용): +217행/cell+881(전수시뮬). **패치6+6b replay 검증(07-08 11:25): 7패치 누적 = cell 37.892→44.356%(+6.46pp), master 6221→8554(50.0→68.8%), code→6796(54.6%), itemName→4513(36.2%), moc 49.95→68.68%(+18.73pp), quantity +549·amount +320·manufacturingNo +353 동반회복(중복행 효과), spurious 0, study 무변화. 단 insuranceCode -78(중복행 재정렬 부작용, 소액 알려진 비용).** 잔여 gap 81.2까지 12.4pp.

**★FT 1차 판정(2026-07-08, run 064): 기각+방향검증.** 라벨 결함=숫자 라벨이 GT정규화형(콤마라벨 86/48256뿐)→모델이 구분자 벗기기를 학습→**콤마-붕괴**: money 정규식 전멸(amount 58.7→4.7%, quantity→8.6%, mfgNo→0.1%, free경로 591→187, HA-append 331→0). rec리포트 exact +22.5pp는 대부분 포맷팅 학습, char-sim은 -2.3pp. **BUT 품명 인식 개선은 실증**: 실패행 3856 모집단서 GT품명 OCR읽힘 84.3→91.2%(+6.9pp, 순+266행), 파이프라인 반파에도 master 50→59.2(+9.2pp). **064 base 승격 금지 — 현행 base=063 replay 7패치(cell 44.36/master 68.8%).** FT 2차 레시피: 품명 크롭 중심(itemName GT=인쇄명, master정식명류 제외)+숫자는 labels_correct 방식(검증크롭+읽은원문 라벨)만+학습후 rec게이트(품명↑ AND 숫자 보합 확인 후 eval). 사용자가 롤백+품명전용 FT 재학습 진행중. 065 판정 3축=인식바운드 전환율/숫자 회귀0/free경로 복원. 064 스냅샷 보존(3-way 비교용).

**★패치8 V5 jamo-trigram(2026-07-08):** trigrams()에 NFD 자모분해 추가(음절→자모 gram). 근거=음절 trigram은 OCR 한글자 오류(캡슬↔캡슐)에 gram 3개가 통째 날아가 B버킷(top30밖) 802행 주원인 — jamo면 한 자모 차이=소량손실. 063 스윕: **전면교체 floor 0.25 = master +384(gain474/reg90) 최적**(0.30 +279/0.35 +75/0.40 -216, H1구제전용 +243/reg0/정밀도71%는 순이익 열세로 미채택). 파급 재보정: MATCH_SIM_FLOOR 0.20→0.25, _SYNTH_SIM_FLOOR 0.35→0.45(등가비~1.25). 검증: patched match=8938 정확재현(기존 8554), code 8240. **주의: pg_trgm 비등가化(match_parity_check 벤치 무효), war 넘어서는 정당한 엔진개선.** **★replay 검증완료(065 replay 07-09 10:53): master 8554→8962(72.0%, 예측+384 초과 +408), code→7117(57.1%), moc 68.68→71.96%, cell 44.356→44.995%(+0.64pp), spurious 0, study 무변화, 타컬럼 ±한자릿수(synth 재보정 유효).** **8패치 누적: master 50.0→72.0%(+22pp), cell 37.892→44.995%(+7.1pp). 81.2까지 잔여 9.2pp(+1141행).** 065=AWS 라이브가 로컬 replay와 완전일치 확인(7패치 배포검증). jamo는 git 배포 대기.

**★itembuycust arc 종료 — P1채택·P2/P3기각(2026-07-10 전수측정):** 공급자 bizno 74→92% 완료(refine_supplier/buyer_bizno, 하드코딩→데이터유도 buyerBranchBiznos, dict 재생성). itembuycust=공급자 bizno→과거구매 item_cd[](build_master.sql, 평가월2606 제외=오염방지, -v ibc_excl_month=0면 전체). MasterMatcher.itembuycust_rescue(작은셋이라 낮은 floor 0.15 안전). fill_master_match(supplier_bizno=) rescue패스: 전역매칭 미달 빈칸만 거래처이력서 재시도. ★순서 재배치: bizno refine→master fill(refined bizno가 앵커). **P1(빈칸rescue) 채택 — 신코드 전체2002 직접replay 실측: master 8962→9019(72.03→72.49%, +57 오프라인예측 정확일치), cell 44.995→45.077%, spurious 0, study 무변화, ibcFilled 총277(그중 GT-blank 미침범).** floor 0.15(0.10=오채움↑, 0.25=+0 jamo흡수). GT-failed트리거(+286)는 배포불가 착시. **P2(regular-match override) 기각**: 커브 near-zero-sum, floor별 gain≈loss(0.50 peak +10=0.08pp뿐, 0.30 −82), 확정매치 덮어쓰기=제로섬 실측. **P3(bizno itembuycust tie-break) 기각**: pool 복수후보 애매 2 docs뿐→+0, 잔여8%는 recognition-bound(OCR숫자오독)라 tie-break 무효. **결론: itembuycust 가치=전부 P1 +57.** ★주의: 파이프라인 fill_master는 정상발화(monkeypatch 300docs→ibc41 확인)하나, replay_compare **디스크는 stale일수 있음**(itembuycust 배선전 run이면 8962 그대로)—검증은 신코드 in-process replay_dispatch 재집계로. **배포대기(사용자몫): 5파일 커밋(master_match/invoice_statement_free/build_master.sql/main.py/replay_compare.py)+master_dict.json scp(gitignore)+prod ibc_excl_month=0 재빌드.**

**★심층 재검증(2026-07-10 "정말 최선?" 3발견, 전부 065 전수실측):** ①**어제 bizno sep-refine에 숨은 study 회귀 −1.39pp**(전부-지점-sep 최후폴백이 study 3계열 4docs서 백제 영등포지점을 supplier 오채움→PartyMatcher가 supplierCompany '예일선'까지 오염. 내가 study cell만 보고 **field macro를 안 본 검증누락**—이후 study 검증은 반드시 field까지). 수정=지점픽에 등록명(biznoToCust→cust nm)↔추출상호 jamo-sim≥0.3 게이트(**빈 상호=반박근거없음=통과**, refine 시점 comp는 원시추출이라 빈칸 다수—파이프라인후 값으로 sim 재면 착시), study 79.609% 정확복구. ②**bizno 무구분자 회수**(sep-only 제한이 남긴 돈): 체크섬(가중 1373713 5)+known셋(supplier=itembuycust 키/buyer=branch셋)+유일 3중가드. 순서=지점픽게이트 먼저→ns rescue(ns에 상호게이트 걸면 −18: OCR변형/표기차로 진짜 fix 차단—미적용). 동일잣대(compare_fields) 최종: **supplierBizno 90.68→91.22(+10), buyerBizno 81.78→88.85(+109), supplierCompany +15, master 72.49%/spurious0/study복구**. ③P2 기각을 원리로 확정: ibc후보=전역사전 부분집합→**ibc_sim≤reg_sim 항상**, margin게이트 수학적 불가. P1 floor 0.15 최적확정(인하 +6 vs 오답+49~115). 잔여 supplier 오류~9%=OCR숫자오독(인식바운드). 롤중요교훈: 게이트 문턱은 반드시 **refine 시점 값**으로 측정.

**★행조립 심층검증=천장 재확정(2026-07-10, 065 전수):** master 실패 3423행 완전분해: 품명빈칸 1800(팬텀1668/adopt132, fallback 73%) + 오배정텍스트557(숫자침범264/시프트69/잡231) + 반깨짐347. **GT오염(제품명//박스번호형 '//3' 등)은 1%(2문서/32행)뿐—452644가 드문 예외, 과일반화 주의.** 회수가능성=빈칸1800 중 GT품명이 OCR라인에 살아있는 974행에 **실제 synthesize 게이트** 적용시: **미소비 콤마금액 없음 270(금액이 기존행 점유/미읽힘)·중복명difflib0.8 226(=619 라인소비 천장)·진짜통과가능 2행뿐.** (내 1차 복제본의 '160 통과했어야'는 used_amts(중복계상 방지) 누락 착시—실제 synthesize_missing_rows는 신설금액이 기존행 amount/unitPrice와 겹치면 거부.) **결론: 행조립 저리스크 룰 승 ≈0. 진짜 레버(중복명226+숫자침범264)는 fallback 파서 라인-소비 재작성=中규모인데 천장도 미읽힌금액270에 recognition-bound. 룰트랙 소진 재확정, 81.2는 파인튜닝 필수.** [[project_free_vs_template_gaps]]

**★war ocr.xml 원본 확인 + 미착취 pull 검증=기각(2026-07-10, 065 전수):** war 실제 매퍼=`_waranalysis/WEB-INF/classes/mybatis/mapper/ocr.xml`(트리에 실존, 유일 원본). 품명매칭=4쿼리(learndata정확→itembuycust→BestLike전체 SIMILARITY+|bp1-단가|→Like clean+LIKE) 전부 우리 캐스케이드와 동일. **매칭 후 insertInvoiceBody 저장필드=item_cd·bohum_cd·product_code·description(정식명), bp1_amt는 저장안하고 ORDER BY tiebreak뿐.** 거래처=selectCustBestLike(service_no+brch_cd+levenshtein), 지점=selectBrchBestLike. **미착취 pull 2건 검증: ①itemCode "표준코드(pyojun) 채워야" 가설 오류→정정: GT itemCode=build_gt.sql `b.item_cd`=마스터 item_cd(8자리우세, GT 100% 실존, pyojun과 16%만 우연), 우리 이미 올바른 코드체계 채움, 57%=매칭정확도(형제SKU갭)지 코딩불일치 아님. 공짜이득 0. ②insuranceCode=GT `b.bohum_cd`: 매칭행 마스터 bohum이 GT와 43%일치(정당 war동작)나 채우면 spurious 못없앰—무게이트 gain1525/spur2908, 문서열게이트 323/178, +코드정확일치 269/164. war가 같은item 매칭해도 bohum 안넣은 GT-빈칸 행이 구조적 존재→예측불가, spurious→0 불가.** 결론: 룰 pull 레버도 정당gain엔 오염동반 벽. **[[project_baseline_matrix_stages]] ocr.xml=easyOCR 아니라 이 매퍼가 실제 매칭. 커스텀모델 여부는 URL만으론 미확정(processorVersions는 범용도 씀).**

**★숫자컬럼=파서 문제지 파인튜닝 아님(2026-07-10 065 전수 직접검증, 내 오류 정정):** 앞서 "숫자 인식%낮음=파인튜닝"이라 했으나 **인식% 지표를 거꾸로 읽은 오류**(인식%=인식/전체결함, 높을수록 OCR바운드; 단가 인식%16%=결함의 16%만 인식, **84%는 parser-drop=읽었는데 흘림**). 직접검증(틀린 숫자셀 GT값이 full_text에 있나): **단가 readable 97%/absent2%, 금액 94%/3%, 수량 99%/1%.** 숫자는 OCR이 다 읽음, 진짜 못읽은 건 1~3%뿐. **숫자 손실=mislocate/drop/wrongpick=fallback 행/열 조립 문제**지 인식 아님. 단 "full_text 어딘가 존재"는 룰회수 쉬움 아님(대부분 mislocate=위 행조립 中규모벽). **교훈: 컬럼별 파인튜닝 vs 파서 판정은 인식% 지표 방향 헷갈리지 말고 GT값 OCR존재 직접측정으로.** 품명raw 읽기 인식%52%/정식명79%=품명은 잘읽힘. 파인튜닝 몫은 **품명 인식(형태소 오독)**이지 숫자 아님. [[project_free_vs_template_gaps]]

**최종 잔여분해(master fail 4094 = 67.1%→81.2 필요 +2166):** 인식-바운드 성격(B top30밖 828 + 라인못찾음 547 + sim미달 201 + not_read 511 ≈ **~2000행 = 파인튜닝 몫**) / 랭킹잔여 E+F+G 793(2차 tiebreak, 고위험 저수익) / floor잔여 566(price무근접, 정밀도 40~50%라 방치) / 구조잔여(중복 619·embedded 245·콤마없는금액 231). **룰/파서 추가 기대치 ~+2\~3pp에 불과 → 81.2 도달은 인식개선(파인튜닝) 없이 산술적으로 불가. 로드맵 🚦게이트 도달.** 파인튜닝 인프라는 [[project_finetune_pipeline_runnable]] 가동가능 상태. [[project_free_vs_template_gaps]] [[project_matching_bench_handoff]] [[feedback_latest_report_analyze_flow]]
