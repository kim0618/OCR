---
name: project_preprocess_image_deskew_gap
description: 변주(각도사진) 실패 근본원인=deskew 각도검출이 표테두리 락온→0°오판. 전처리 선결 플랜
metadata: 
  node_type: memory
  type: project
  originSessionId: f649e999-a806-4625-8cd2-8df7f15844df
---

**근본원인 (run 006 24장, 2026-06-11 확정):** 각도로 찍은 변주 JPG들이 표 숫자가 행을 넘나들며 깨지는(예: 5-2.jpg `quantity="100 400,000"`에서 400,000은 다음 행 값) 진짜 원인은 파서가 아니라 **전처리(기하 미보정)**.

**정확한 메커니즘 (main.py 2438~2502 코드 확인):**
- deskew()는 **이미지에도 실행됨**(2440 `deskew(doc_img)`). "이미지는 deskew 안 한다"는 틀린 표현.
- 변주 전부 `deskew applied=false, normalizedAngle=0.0` → deskew가 **기울기 0°로 오측정하고 스킵**. 코드 주석(2470) 자백: "deskew()의 minAreaRect 각도가 **표/테두리에 락온**". 즉 각도검출이 표 테두리에 락온돼 기운 사진을 0°로 오판. (원근왜곡 동반 여부는 P0서 측정)
- `fileType=="image"→"image_not_target"`(2455) 분기는 deskew 실행이 아니라 **over-apply 되돌림 가드(PDF 전용, 7.pdf 過보정 수습 3O산물 [[project_deskew_overapply_chain]])** 에만 있음. PDF/이미지 구분은 본질 아님 — PDF도 비트맵 렌더 후 OCR.

**측정으로 가른 것:** 오류 419건 중 룰대상(OCR읽음)170 / OCR바닥(안읽음)249. 숫자컬럼(수량34·단가16·금액16)=룰대상이나 뿌리는 전처리 기하. itemName(바닥75)·주소·대표자·사업자번호=OCR 바닥=룰 불가.

**합의 방향:** 전처리 **선결** 후 실데이터. 안 잡고 흘리면 오류가 전처리 잡음에 묻힘. **게이트 플랜(P1 삭제됨):** P0 계측(deskew 0°오판 확인+토큰baseline 진짜기울기+잔여tilt↔셀상관)→P2 실패특정→P3 최소패치(deskew 각도소스를 토큰baseline로=테두리락온 회피 + over-apply 가드 PDF/이미지 통일; **deskew 함수 코어는 불변 유지, 우회**)→P4 재측정(사용자 실행, base 무회귀 ∧ 변주 개선)→P5 실데이터. ~~P1(각도 GT)~~ 불필요=기준은 base 정립, 이미 셀-vs-base로 채점됨.

**자기완결 핸드오프 문서:** `OCR/docs/PREPROCESS_DESKEW_PLAN_20260611.md` (새 채팅방은 이것부터 읽으면 P0부터 시작 가능 — 근본원인·베이스라인·이번세션 끝낸 것·플랜·경로 전부 포함).

**P0 결과 (2026-06-11, 재실행 불요로 확정):** 새 측정 스크립트 만들 필요 없었음 — run 006 `samples/*.json`에 `preprocess.deskew`(rawAngle/normalizedAngle/applied)가 **이미 저장됨**. (교훈: 측정 필요 시 기존 run 산출물부터 확인. 내가 만든 probe_deskew_tilt_p0.py는 과잉이라 삭제함.) 기존 데이터로 ① 확정: **deskew 24장 전부 applied=False**(단 한 장도 기울기보정 안 함). 변주 18장 거의 다 `rawAngle=90.0→norm=0.0` = minAreaRect가 표 세로선/테두리(90°)에 락온→0°오판→스킵. 그 변주 cellAcc=0.0~0.2 바닥 vs 정방향 base=0.5~0.97. (4.pdf만 norm=2.619인데 applied=False=3BF over-apply 가드가 되돌림, 메모리 [[project_deskew_overapply_chain]] 일치.) **③ 상관도 그룹대비(변주 락온스킵→바닥 vs base 정상)로 사실상 확인 → '전처리가 원인' 정량근거 충분. P0 게이트 통과, P2(실패 특정)로 진행 가능.** 토큰기울기 연속변수(진짜 몇 도)는 run006에 없지만 n=24 소표본에 과한 정밀도 — 필요하면 P3 패치 검증 때 최소 측정.

**P2/P3 정정 (2026-06-11, 픽셀 직접확인으로 가설 수정):** deskew 회전각 교체(P3) 패치를 만들었다가 smoke에서 **measure_skew_angle이 orient 보정 후에도 변주 9/9 중 8장 0.0 반환**(투영-프로파일이 이 변주들 기울기 못 잡음) → 패치 무효 판정·원복. OCR 입력 픽셀을 직접 덤프(detect_document+기록orient 적용, 엔진없이)해 본 결과: **deskew 락온은 진짜원인 아님.** 변주엔 고칠 회전기울기가 거의 없음. **진짜 지배원인 = `detect_document`(preprocess.py:42) 원근보정 스킵.** 18변주 전수(엔진없이 detect_document 상태+기록orient+cellAcc 조인): detect_document SKIP 10장중 9장 cellAcc 0.0~0.2 / OK인 1-x 3장 전부 0.78~0.88(정상). 스킵조건=`best_area>0.85*hw 또는 가장자리 borders>=3`(preprocess.py:81) → 종이가 풀프레임인 사진에서 보정 가장 필요할 때 포기. 2차원인=orientation 오판(6-2 텍스트 90°누움인데 orient=0 기록) + 일부 OK인데실패(3-1/3-2/6-3). **다음 레버 = detect_document 스킵조건(원근보정), deskew 아님. 단 detect_document 변경은 영수증 BASELINE 회귀위험(스킵가드가 영수증엔 옳음) → 스코프·무회귀 게이트 필수.** [[project_data_storage_architecture]]

**P3' 실험 (2026-06-11, env-gated, 사용자 실행 대기):** detect_document 손보기 방향 확정(사용자 선택)이나, 검출사각형 기하상 깔끔한 SKIP/OK 판별자 없음 — SKIP 10장이 ①진짜키스톤문서(워프하면 회복 가능: 5-2 -5°, 7-1 -12°/H15%, 7-3 +12°/30% 등 ~6장) ②테두리오검출(4-1 area100% all-zero=화면테두리 잡음, 워프=항등 회복불가)로 갈림. 그래서 추론 대신 **경험적 측정**: `preprocess.py:detect_document` 스킵 분기에 **env-gated 강제워프** 추가 — `OCR_FORCE_WARP_ON_SKIP=1` 일 때만 스킵 대신 검출사각형 워프. **env 미설정=기존 100% 동치(baseline 무회귀)** 검증됨(OFF: 5-2 스킵·1-1 완료 그대로 / ON: 5-2 워프 2619x4021, 4-1 항등 99.9%). 백업 `backup/preprocess_20260611_before_p3prime_forcewarp_experiment.py`. **실행(사용자, 서버 프로세스에 env):** 서버 끄고→`$env:OCR_FORCE_WARP_ON_SKIP="1"` 세팅 후 서버기동→`eval\run_all.py`(study 라이브 재OCR). **주의: study만. 이 env 켠 서버로 baseline/영수증 돌리지 말 것(영수증도 워프됨).** Gate: SKIP변주 cellAcc↑ 확인되면 그때 스코프+baseline 게이트로 커밋. 안 오르면 detect_document도 레버 아님→재검토. [[project_data_storage_architecture]]

**P3' 측정결과 (2026-06-11, run 008 vs 006, 강제워프 ON으로 실측 후 클린 원복):** **레버 확정 + 무조건적용 불가.** ✅원근보정(detect_document 워프)이 각도 JPG에 정답: 5-2(0→0.6)·5-3(0→0.567)·7-1(0→0.333) 실회복=개입효과로 '전처리가 원인' 증명. ❌단 무조건 워프는 깨끗한 문서 회귀: **큰 회귀 전부 PDF**(5.pdf 0.967→0.6, 3.pdf 0.75→0, 4.pdf 0.6→0.2). PDF렌더=화면 정당히 채우는 디지털문서→검출사각형이 페이지테두리→워프시 키스톤 손상(스킵가드 존재이유). 전체 cell 0.723→0.747 올랐지만 **함정(개수평균, 잠긴 base 손상은 불가)**. **판별자: 이득=각도JPG사진 / 큰회귀=PDF렌더.** **다음 패치(커밋후보)=원근보정 스킵분기를 image(비PDF) 입력에만 적용**(main.py `_is_pdf_input` 이미 있음). 단주의: 1.jpg(base 이미지) -0.04, 6-2(이미지) 0.2→0 = image그룹내도 소수 잔여 → image한정 후 재측정 필수(base PDF 복구 ∧ 변주 유지 ∧ 1.jpg 회귀 허용범위 확인). 스위치 클린원복됨(backup/preprocess_20260611_before_p3prime_forcewarp_experiment.py 기준). [[project_data_storage_architecture]]

**P3 커밋 확정 (2026-06-12, run 010 vs 009 계측baseline):** `detect_document(image, force_warp_on_skip=False)` 파라미터 추가(기본 False=무회귀), main.py가 `force_warp_on_skip=(not _is_pdf_input)` 전달 = **image만 스킵가드 우회 워프, PDF는 스킵 유지**. 백업 `*_before_p3_image_only_perspective.py`. **결과: ✅base PDF 무회귀 완벽(3/4/5/6/7.pdf 전부 +0.000, run008 참사 해결) ✅변주 5장 회복(5-2 +0.6, 5-3 +0.567, 7-1 +0.333, 4-1·4-3 +0.2) ✅structure 결함 221→158.** ⚠️6-2 회귀(0.2→0): orientation 누움 케이스에 원근워프 겹쳐 악화=P3범위밖. ⚠️1.jpg -0.041(미미). ⚠️1-2.jpg None=OCR 300s 타임아웃 flake(P3무관, 재측정필요). **게이트 '변주개선∧base PDF회귀0' 달성→P3 커밋.** 결정: 6-2/3-1/3-2/6-3 = **orientation 결함 버킷**(detect_document 완료인데 깨짐)→다음 레버는 orientation으로 분리(한번에하나). **계측 갭(이슈2):** P3가 모든 image skip=False로 만들어 buckets `docSkippedOnImage` 신호 무력→워프후실패 변주(5-1/6-1/4-2)가 preprocessing버킷서 빠짐(10→1). 해결=forcedWarpOnSkip 신호 추가(아래 [[project_eval_loop_preprocess_aware]]).

**orientation 레버 진단 (2026-06-12, run 011, ⚠️우회 어려움 확정):** orientation 오판 4장 픽셀 직접확인 — 3-2(0°인데 90°누움)·6-2(0°인데 누움)·6-3(180°인데 뒤집힘)·3-1(270°). 신뢰신호(firstPassDiff/ratio) 분석: **저신뢰 오판은 6-3(diff2.5)뿐, 3-2(diff25/ratio4.1)·6-2(diff22.8/ratio29.5)는 strong_portrait로 *확신하며* 오판.** → "저margin 재검증" 우회로 3-2/6-2 못 잡음. 원인=detect_orientation 점수함수(한글1.2+숫자0.9+line0.8+conf)가 가로문서-세로촬영에서 0°(portrait) 강선호(표 숫자/짧은텍스트가 회전강건→0°서도 점수↑)+strong_portrait 조기확정. **결론: orientation 수정=코어(Do-Not-Modify) 손봐야=영수증 baseline(방향 잘잡음) 회귀위험. probe 6장 근거로 강행 위험.** 계측 보강(run011): forcedWarpOnSkip 신호로 전처리버킷 1→7 재포착(워프후실패 변주), 1-2 타임아웃 해소, cell 0.767. 단 orientation부류(3-1/3-2/6-3, detect_document 완료라 perspective candidate 아님)는 여전히 버킷밖=orientation 분류신호 별도 필요.

**T-27a-fix3 레버 구현 (2026-06-12, 측정대기):** orientation 오판(bailout)을 영수증 안 건드리고 고치는 길 확정. **핵심 발견: eval(`/ocr/extract`, templateMode=unstructured, region 없음)은 `else` 완전비정형 분기(main.py~2353)를 타고 거기 detect_orientation(2386)이 bailout 출처.** 기존 비활성 T-27a-fix2 anchor fallback(2177)은 `if region_list:` 기본템플릿 분기 안이라 **eval 경로 안 탐**(재활성화해도 무효). → fix3을 **else 분기 2386 직후**에 신규 삽입: **게이트=documentType=="invoice_statement" 단일**(영수증은 태그 없어 자동 제외, env 안 씀 — 사용자 요청 [[feedback_no_env_flags]]), anchor(15개) 4방향(0/90/180/270) OCR카운트 비교, best≥c0+2 ∧ best≥4 ∧ ≠0 일때만 회전(보수적). **무회귀: 영수증=invoice태그없어 정의상 안탐 / core detect_orientation 불변(우회).** strictly additive, debug=`preprocess.orientation.anchorOrient`. 백업 `backup/main_20260612_before_T27a_fix3_unstructured_invoice_anchor_orient.py`. **실행(사용자): 서버 재기동(9099)→eval run_all(study). 거래명세서엔 자동 ON.** 게이트: 6-3(180)·3-2/6-2(90)·3-1(270) 회복 ∧ base 거래명세서 무회귀면 커밋. 안오르면 anchor신호 부족 재검토. [[project_invoice_unstructured_roadmap]]

**T-27a-fix3 측정결과=실패·원복 (2026-06-12, run 013 vs 012):** 게이트 2개 다 실패 → backup에서 main.py 원복(012 복귀). **①타깃 못 고침:** 6-1/6-2/6-3 anchorOrient checked but NOT applied — 회전방향은 맞게 찾음(6-1·6-3 best=180, 6-2 best=90)이나 **각도사진을 768px thumb로 OCR하면 회전본에서도 anchor가 최대 3개**(c0=0, best=3)라 보수적 임계 best≥4에 3<4로 막힘. 임계를 낮추면 false-rotation 위험. **②멀쩡한 거 깨뜨림(치명):** 4방향 = 이미지당 OCR 최대 4회 추가 → 28행 무거운 1-2.jpg·1-3.jpg가 **300s ReadTimeout(status=error, path=unknown, 0행)**. run012 1-2=87.6%·1-3=85.1% → 013서 0%. 변주 셀 73.3→59.6 회귀. **교훈: (a) anchor 카운트는 각도/저해상에서 신호 빈약(3 천장) → 4-way anchor 방식 자체가 이 변주엔 부적합. (b) 전처리 단계에 풀 OCR 추가는 timeout 비용 과다 — orientation 판정에 ocr.ocr() 반복 금지.** 다음 시도 방향(미정): orientation은 OCR-free 신호(텍스트라인 각도/투영, detect_orientation 내부 점수 재활용)로 가야지 anchor 재OCR은 아님. core detect_orientation 점수오판이 진짜 병목인데 그건 Do-Not-Modify라 여전히 난제. [[feedback_no_env_flags]]

**O0 패치 (2026-06-12, 측정대기, 사용자승인):** detect_orientation 코드 직독으로 진짜 원인 확정 — 각도 거래명세서는 first_pass=(0,180) 신호약함→`low_signal_portrait_bailout`(preprocess.py:350) 또는 `strong_portrait_first_pass`(348)로 **second_pass(90/270) 안 보고 early-stop→0° 오판**(정답방향 채점조차 안함). + 호출(main.py:2386)이 target_short=224인데 docstring(164)은 invoice엔 512 권장(미사용). **패치=additive only**: preprocess.py `detect_orientation`에 `force_full_eval=False` 파라미터 추가(True=early-stop 전부 끄고 4방향 채점 후 max), main.py:2386 invoice일때만 `force_full_eval=True,target_short=512` 분기(비invoice 바이트동일). 디버그 `preprocess.orientation.allScores`(방향별 score)+forceFullEval 노출. 백업 `backup/{preprocess,main}_20260612_before_O0_*`. **비용 정정(run014 실측): 512px+4방향이 너무 무거워 1-2/1-3/3-1/3-2 300s 타임아웃(1-1도 202s 턱걸이) → target_short=512 철회, 224 유지(force_full_eval만).** 224에서 4-pass=디폴트 2-pass의 ~2배(작은 thumb라 감당가능 가설). Gate O0: 7장 0%변주(6-1/2/3·3-2·3-3·4-2·5-1) allScores에서 정답방향이 score최대면 score함수 충분→O1, 아니면 score함수 부족=비OCR신호 필요. Gate O1: base무회귀∧변주셀↑∧1-2/1-3 타임아웃없음. 실행=서버재기동→run_all(env없음).

**O0/O1 결과=메커니즘 검증 성공 (2026-06-12, run 015 vs 012, 224 force_full_eval):** **3관문 통과.** ✅타임아웃: 1-2/1-3 정상(0.876/0.851). ✅**base 무회귀 완벽**: base6·PDF·1-x 전부 1비트도 안변함(변화는 변주에만 국한). ✅O0: 신호 충분하면 정답방향 정확히 찾음 — 6-2(allScores 90°=61 압도적→0→0.462 ✅✅), 3-2(270°=62.8→0→0.125), 5-1(90°→rows1→6복구→0.167), 6-1(270°→rows2→6복구→0.077). 필드도 47.1→50.0%. **순효과 +4승−1패.** ❌회귀 1장=4-3(0.2→0): 224 약신호(allScores max 11.2)에서 270°오선택(정답 180°=0.8). 미복구 3장=3-3(전orientation 0점)·4-2(max5.0)·6-3(12.4vs9.9 박빙)=전부 224 신호빈약. **결론: 잔여문제(4-3회귀+3-3/4-2/6-3)는 전부 "224px 신호 약함" 하나. 그리고 이 4장은 전부 가벼운 단일행=512 올려도 타임아웃 안남(타임아웃은 28행 1-2/1-3뿐인데 걔넨 신호강해 224유지).** [[feedback_local_cpu_vs_gpu_prod]]

**O2 패치 (2026-06-12, 측정대기):** 2단계 escalation. main.py orient호출에서 224 force_full_eval 후 **최고 score<30이면 512px로 재채점**(회전전 원본서 다시). 무거운 28행=score높아 escalate안됨(타임아웃회피), 약신호 가벼운 변주만 512. 디버그 `escalated512`. 백업=O0과 동일(`main_20260612_before_O0_*`). Gate O2: 4-3 회귀해소∧3-3/4-2/6-3 복구∧base·1-x 무회귀∧타임아웃없음. 실행=서버재기동→run_all.

**O2 폐기·원복 (2026-06-12, run 016 타임아웃):** 2단계(score<30→512)가 **각도 28행 변주(1-2/1-3)도 잡아버림** — 각도 무거운 장은 224 썸네일서 글자 안읽혀 score 낮음→512 승격→28행 4× OCR→300s 타임아웃. "무거운 장은 score 높아 escalate 안됨" 가정이 틀림(각도 무거운 장=224 저score). **로컬 CPU에선 512를 무거운 장에 못 돌림.** escalation 제거→**현재 코드=run015 상태(224 단일 force_full_eval)**. **로컬 최종 결과 확정=run015: +4승−1패(4-3 −0.2 weak-signal 오선택)·base완벽·타임아웃0.** 224에선 weak-signal 좋은픽(6-1/5-1)과 나쁜픽(4-3) 분리 불가=512 필요한데 그건 GPU전용. **결정: 224 force_full_eval을 로컬 end-state로 유지(재실행 불요, run015가 곧 현재코드 결과). 512 단일적용(4-3해소+3-3/4-2/6-3복구)은 GPU 배포 때.** 커밋 여부=사용자 판단(net+4/−1, base안전). [[feedback_local_cpu_vs_gpu_prod]]

**헤비독 orientation 가드 + force_full_eval 확정 (2026-06-12, run 018):** force_full_eval(4방향)이 28행 무거운 1-x를 300s 타임아웃시킴(run017) → **줄수 가드**: default 먼저 돌려 first_pass_line_count>=20(무거운 다행=default 신뢰)면 force 안 함, <20(가벼운 단일행 변주)만 force_full_eval 재채점. 무거운장 default1회·가벼운변주 default+force(둘다 쌈)→타임아웃0 ∧ 변주교정(6-2 0.462 등)유지. main.py:~2393. 512는 여전히 GPU전용. (로컬 CPU 비용 줄다리기는 [[feedback_local_cpu_vs_gpu_prod]] — 게이트는 정확도지 속도 아님.)

**P1 buyer-block 파서 수정 확정·committable (2026-06-12, run 018 vs 015):** 증상=공급자는 잡고 buyer 블록(사업자번호/주소/대표자) 통째 누락(6문서, 특히 1.jpg족). **근본원인=전역 `_BIZ_RE`(invoice_statement.py:6)가 그룹사이 구분자 [-\s.]를 필수로 요구→대시없는 raw 10자리 buyer 번호(1138504425) 못 잡음→`_biz_candidates`가 후보1개만→party 분기(len(bizs)>=2) 안돌아 buyer 미지정.** probe(`tmp/probe_1jpg_buyer_bizno.py`)로 upper_block OCR에 번호 실재 확인=인식바닥 아닌 파서문제 확정. **수정=전역 _BIZ_RE 불변, `_biz_candidates`(상단영역 한정)에서만 raw 10자리도 후보 인정**(phone/12+자리 경계 배제). 백업 `backup/invoice_statement_20260612_before_P1_buyer_rawbizno.py`. **결과: buyerBizNumber +16.7·buyerRepresentative +20.0·supplierAddress +10.0(주소 party분리 덤)·회귀0·필드 50→54.1%.** buyerAddress 0% 잔존=주소 꼬리노이즈(자상으로)=P3 별건. **교훈: party 앵커 시스템은 양쪽 사업자번호 detection에 의존 — 한쪽 형식(raw vs 대시) 놓치면 그 party 통째 침묵.** 다음=P3(주소경계) 또는 P2(행그룹핑).

**orientation force_full_eval → GPU 게이트 확정 (2026-06-15, run 022):** force가 무거운 28행 1-x를 4-pass OCR로 300s 타임아웃 반복(run014/16/17/21). 무거운 장을 크기·줄수·시간 어느 신호로도 못 가림(측정: 가벼운 3.pdf orient 35s > 무거운 1.jpg 20s). → main.py 상수 `_ORIENT_FORCE_ON_GPU=False`(env아님 코드게이트)로 **로컬 기본 OFF**, GPU 배포 시 True(+512검토). 백업 `main_20260615_before_orient_force_gpu_gate.py`. **부수효과(중요): force 끄니 셀 74.3→76.7(+2.4)·구조오류 165→141(−24)** — force가 변주 행복구하며 동시에 표를 흐트려 구조오류 양산 중이었음. 즉 로컬 정확도에도 force off가 맞음. 변주 orientation 회복은 GPU 몫. [[feedback_local_cpu_vs_gpu_prod]]

**P3-a cross-party 주소오염 수정 (2026-06-15, run 021/022):** 증상=base PDF(OCR완벽)도 buyer주소에 공급자 지역머리(`경기도부천시원미구`) prepend. **근본원인=`_apply_address_continuation_post`(invoice_statement.py~3686)의 PREFIX continuation이 prepend 전 상대 *biz*만 체크하고 상대 *주소*는 안 봐서, prefix_missing 판정시 공급자 지역줄을 buyer 앞에 붙임.** (첫 시도 `_strip_overlapping_party_address` Strategy2는 오염이 *생기기 전* 단계라 헛돌았음.) 수정=prepend 후보가 `_party_norm(opposite.address)`에 포함되면 거부. 백업 `*_before_P3a_continuation_prefix_guard.py`. 검증: 5.pdf buyer `경기도부천시원미구 서울특별시…`→`서울특별시…`. **단 디버깅 교훈: 서버 중복기동(venv+시스템python 동시) + .pyc로 stale 바이트코드가 여러번 헛run 유발 — 변경 무반영시 `Get-NetTCPConnection -LocalPort 9099`로 서빙 pid 확인 + 중복 kill + pyc 삭제. main.py는 uvicorn.run에 reload 없음→`python main.py`는 자동reload 안됨, --reload로 띄워야.**

**P3-b 주소 정규화 (2026-06-15, run 022 재채점):** P3-a가 주소 *내용*은 고쳤으나 채점이 띄어쓰기/괄호로 mismatch(`공원로8길24` vs `공원로8길 24`). 주소 불일치 30건 중 **12건이 SPACING_ONLY(값 정답, 공백·괄호만 차이)**. 수정=`eval/normalize.py`에 `norm_address`(공백·괄호·구두점 제거=`_NON_ALNUM`+casefold) 추가, supplierAddress/buyerAddress만 FIELD_TYPE "text"→"address"(상호/대표자/품명 등 text 전역 불변). 백업 `normalize_20260615_before_P3b_address_norm.py`. **결과(재OCR 없이 `run_all --reuse <ts>/study` 재채점): supplierAddress 10→40%·buyerAddress 0→25%·필드 51.2→56.1%(+4.9pp)·셀 불변·checker PASS·normalization-golden 유지.** 정당한 측정교정(한국주소 띄어쓰기 가변, 글자오류는 안건드림). **재채점=재OCR불요(스코어링만 바뀔 때): `--reuse`는 ts에 `/study` 서브디렉토리 포함해야 함(`runs/ts/study/samples` 구조).** 다음=P3-c(REAL_DIFF ~15: cross-party잔여 4.pdf/7-x+변주garbled) / P2(supplierRep 35%).

**주의:** deskew/orientation=보호로직(CLAUDE.md Do-Not-Modify). orientation logic은 명시적 Do-Not-Modify → 레버 진행 전 사용자 승인·게이트 필수. deskew 함수 알고리즘/threshold(0.5)는 그동안 불변으로 지켜옴 → 내부 수정보다 각도소스 교체로 우회. 파서 룰(170건)은 전처리 안정 후 재평가(상당수 자동 소멸 예상). [[project_learn_loop_infra_plan]] [[feedback_user_runs_not_me]]
