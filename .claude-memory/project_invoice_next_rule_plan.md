---
name: project-invoice-next-rule-plan
description: 숫자열 7층 마감 후 다음 룰작업 계획 — ①합계 3형제(산술 자가검증 이식) → ②품명/master 트랙 재개(공식 81.2% 목표) → ③spec
metadata: 
  node_type: memory
  type: project
  originSessionId: a2b7872e-991d-42d1-a975-6de0493723a9
  modified: 2026-07-22T03:19:52.816Z
---

2026-07-15 확정 (숫자열 룰트랙 커밋 9bfe304e6 마감 직후, 사용자 승인).

**순서:**
1. **합계 3형제(totalAmount·taxAmount·supplyAmount)** — 현재 48~52%, parser-drop 각 ~2,000. 오라클 = `공급가액+세액=총액` (war GT 생성식이 supply=total−tax라 GT 자기일관 ~100% — 행 트리플과 동일한 순환-면역 산술). 룰 모양: 셋 중 2개 읽으면 3번째 유도, 3개면 상호검증·오독교정 + 합계밴드(문서 하단) 위치앵커. 문서당 1값 필드라 게이트 깔끔. 숫자열 7층의 검증 하네스([[project_invoice_numeric_rules_p1p2p3]]) 재사용.
2. **품명/master 트랙** — 목표 구글 81.2%. **진행중(2026-07-20, 미커밋)**: master 72% → **73.9%**.
   - ✅ drop 밴드입양 `adopt_band_names_master_gated`: 품명 빈 행에 밴드 유일 한글라인을 마스터 검증(sim≥0.5,단가힌트) 후 fill. itemCode 정밀도 90.4%. +140 master. (게이트無/형태소완화/모호해소 전부 기각)
   - ✅ wrongpick RV1(같은 함수 replace 분기): 현재 이름이 **마스터 무매치(쓰레기/훼손판)**인 행만 밴드 best-sim 후보(sim≥0.5)로 교체. +339 master·+351 raw·+1088셀·study+1. RV2/RV3(이미 맞는 걸 흔듦) 1.0~1.3:1 기각. 형태소 가드도 기각(31:1 GAIN 깎음). **+20 rawCorrect_masterWrong은 회귀 아님**(사전 미보유약 raw개선 구간이동, 핸드오프 게이트=행ID지 bin수 아님).
   - **945 오교정 방어(step6) = 기각 확정(2026-07-20 실측)**: 거부는 정답매칭 10:1 순손실(오교정 92%·정답매칭 62%가 margin<0.05, 분포 겹침). 오교정 정체=형제SKU 41%(규격만 다름, spec바운드)+다른공식명 59%(제조사접두 명명갭). margin/짧은품명/가격 가드로 실현 불가 → **매칭룰 아니라 spec/명명 문제.** mislocate 543만 소형 잔여.
3. **spec(규격) — 착수(2026-07-21)**. 컬럼별 룰-회수가능 헤드룸: **spec 21,584(parser-drop #1)** > manufacturingNo 17,092 > insuranceCode 14,764. ★**실측 리프레임(066 thin 전수)**: 계획의 "16k 경량 num+단위 룰"은 과대추정 — 실제 분해: (B)itemName 임베디드 ~6k=경량룰 대상 / (D)밴드-영역 드롭 ~9k=파서수술·행검출과 겹침 무거운 트랙 / DEAD-행 ~4.3k=spec아닌 행검출 / A)checker 정규화 비대칭(GT`100 c`vs ext`100c`) ~580=스코어링 의미변경 승인필요. **drop rowLIVE에서 GT spec가 itemName에 실재=40.7%뿐**(꼬리8%+부분33%), 59%는 이름 밖. **spec GT는 산술오라클 無 구글OCR 원본이라 비일관**(같은 이름꼴에서 `100T`도 `PTP`도 정답, dosage 붙임 `20mg 30t`, 이름과 불일치 `100t` when name=`40T`) → 어떤 깨끗한 추출도 정밀도 55~66% 천장.
   - **✅ B 완료·공식루프 검증(2026-07-21, 미커밋=git은 사용자)**: `fill_spec_from_item_name`(invoice_statement_free.py) = itemName의 "숫자+개수단위"(정/캡/T/C/P/tab/cap/ea…, **dosage mg/ml/g 제외**) 토큰을 **빈 spec에 COPY**(itemName 불변 — GT itemName이 spec 포함 61.5%라 split-off하면 품명회귀). main.py 합류점(밴드입양 뒤·마스터매칭 앞)+replay_compare 미러. **★핵심가드="공백/괄호 선행 별도 토큰만"**(`(?<=[\s(\[])`): study(사람 golden)와 thin(war/구글) spec 컨벤션이 count-unit **부착형**("정15P")에서 충돌 — thin은 spec분리·study는 이름유지+spec빈칸. 부착형 채우면 study 24 spurious(지어내기 아님, golden 미기록). 공백-선행형에선 두 GT 일치. **★공식(066 replay, study+thin): thin spec셀 25.0→29.6%(−1,701 defect: drop 13668→10531), 전체셀 49.97%, thin+study spurious 0, study Δmatch 0(git base 957 완전불변), master net +26(rawWrong→Correct+10·rawCorrect→Correct+16 = master_match.py:407-409가 spec를 dose/pack/paren 토큰으로 읽어 형제SKU disambiguate=계획 예측 부산물 실현).** 타 컬럼 회귀 0. 3파일 +66줄. **미채택**: 부착형 포함(thin +1795 더 되나 study 24 spurious=게이트 위반, GT의미결정이라 사용자 몫으로 보류), trailPkg(FIX 감소), dose_composite(정밀도 17.7%).
   - **❌ C(wrongpick 정리) = 측정으로 기각(2026-07-21)**: 안전 식별 가능한 garbage는 `mfgNo_copy`(spec==제조번호 정확일치) 62건뿐(교체 21 FIX). lotcode(916)·itemName_blob(327)은 GT spec가 lot코드꼴(`30tab-1bot`)·합성꼴을 정당히 가져 **정답 334건 오탐** → 분류 불가. spec GT는 "어떤 모양도 될 수 있어 쓰레기 규정 불가"(B 교훈 재확인). 21셀=두더지잡기라 스킵.
   - **✅ A(checker 정규화) = 완료·검증(2026-07-21)**: eval/normalize.py `norm_spec` 신설(spec type `text`→`spec`), 전체 비-alnum 제거+casefold(norm_name/address 동일). GT `100 C`·`(병)30T`·`/30Tab.` vs 파서 `100C`·`병)30T`·`/30Tab`·검수마크 `30T √` = 같은 규격인데 text norm이 공백/구두점 남겨 오불일치하던 것 해소. **066 실측: thin spec셀 mismatch→match +1,132(29.6→32.6%), LOSE 0, study 0/0, 오병합 0(전부 순수 공백·괄호·마침표·√junk 차이).** ★checker 전용(프로덕션 main.py는 eval/normalize import 안 함=AWS 배포 불요), 스코어링 의미변경이라 사용자 승인받음. 공식 artifact는 표준루프 재run으로 갱신(replay_compare가 새 norm으로 재계산).
   - **✅ B2(행 _rawText 스캔) = B 확장·검증(2026-07-21, 사용자 "30%가 최선이냐" 재반박으로 발견)**: `fill_spec_from_item_name` 을 itemName-only → **행 `_rawText` 전체 스캔**(폴백 itemName)으로 교체, count-unit 부착형도 허용(공백가드 제거). ★내가 "itemName 밖 59%=무거운 D"로 오판한 게 틀림 — 250장 replay 실측: 빈-spec 742행 중 **GT가 _rawText에 51%**(품명엔 28%). _rawText scan FIX 148 vs name 61(**2.4배**). **★공식 전체(066 replay): thin spec 25.0%→41.3%(15,600/37,810, +16.3pp), 전체셀 49→51.30%, thin spurious 0, master net 양수(rawCorrect_masterWrong 928→892=오매치↓), study 89.94% 불변·회귀 0.** (400장 표본 40.0%보다 전체가 좋게 나옴.) regex `(?<![A-Za-z0-9])`(any-position). ml/mg는 제외 유지(+7뿐·BREAK폭증). study spec_spurious 29(불변매치 98/116).
   - **★study 24~36 spurious = 컨벤션차(날조 아님, 문서확인 완료)**: 5.pdf(품명\|품목코드\|수량\|단가\|금액)·6.pdf(수량\|LotNo\|유효일자\|NO\|제품코드\|제품명) 둘 다 **규격 컬럼 자체가 없음** — `30ML`·`15P`·`100T`는 품명에 인쇄. 사람이 spec 비운 게 옳음. war/구글 Document AI 는 이름에서 쪼개 spec 필드에 넣는 컨벤션이라 thin GT는 채워짐(그래서 thin spurious 0=순환). **war 파리티가 목표(baseline_matrix 기준)라 B2 채택, study 발산은 6장 프로브 컨벤션차로 문서화. ★study golden spec 보정은 철회**(없는 컬럼 지어내는 것이라). 
   - **★spec 트랙 마감(2026-07-21, "41%가 최선?" 재재반박까지 소진)**: 정확일치 **41.3%=룰 천장 확정**. 실질 분해(37,810셀): exact 41.3 + **부분포함 14.4**(우리 `80T`⊂GT `375mg 80T`=정보 맞음, 경계만 구글 임의) = **정보정답 55.6%** / 미추출 30.3 + garble 14.1 = recognition·행검출. classifier "recoverable 15,568"은 spec에선 과장(밴드에 글자 있음≠룰이 판별 가능). **추가 시도·전부 실측 사망**: ①밴드입양(품명입양 인프라 재사용) 정밀도 38.7%·물량77 ②합성형 무조건 GAIN234/LOSS1602 ③합성형 슬래시-조건 GAIN130/LOSS380 — **같은 `16mg/30T`에서 GT가 count-only도 합성도 나옴=판별자 부재** ④컬럼간 이동(mislocate) 92건뿐+GT spec=단가값 노이즈 ⑤liq(ml/mg) FIX+178/BREAK+2684. 부분포함 14.4pp 회수=구글 경계결정 모방=learndata/FT 몫(사용자 보류 카드).
4. **insuranceCode 착수(2026-07-21) — spec와 달리 진짜 오라클 있음**: GT 지배형=9자리 EDI 보험코드, **master_dict item에 bohum(9자리)+pyojun(13자리 바코드) 실재** → ②G4가 채운 itemCode로 join하면 공짜. 실측 우선순위: **M1 join 빈칸fill**(FIX 5,728·spurious 0=GT빈칸 조합 자체가 0) > **M2 이중게이트 교체**(whitelist 밖 AND 9/13형식 불량일 때만, FIX 1,965/BREAK 83=24:1 — whitelist만으론 BREAK 144, 형식게이트가 신약 정답코드 보호) > P1 유일9자리런 정규화(~2,406, M2와 중복多—잔여 재측정 필요) > P2b 밴드스캔(풀 12,623, 후순위).
   - **✅ M1+M2 구현·검증 완료(2026-07-21, 미커밋)**: `fill_insurance_from_master`(master_match.py, whitelist 캐시+`_ins_clean_format`+문서단위 pyojun 선호=깨끗한 13자리>9자리 셀 수), main.py 합류점(fill_master_match 뒤)+replay_compare 미러. **전수검증: FIX 6,750·BREAK 83(81:1)·진짜 spurious 0·study 원천 회귀불가(rich GT에 insuranceCode 키 0행=채점 밖)**. ⚠️검증중 발견: GT행에 키 없는 행 11,011에도 채움=checker 비가시(spurious 아님, `_value_keys`=GT키 기준) — 프로덕션 실익만 있고 측정엔 안 잡힘. wrongpick 쓰레기의 정체=행번호 접두 병합(`1`+`693901210`)·itemCode+보험코드 병합·코드+상호 blob.
   - **★공식 확정(2026-07-22 run)**: insuranceCode **16.0%→45.7%**(10,372/22,678, +29.7pp), thin 전체셀 51.30→**53.15%**, spurious 0, study 957 불변(insuranceCode 채점 밖), master cross 완전동일. defect 18,499→12,306. **P1 추가 격리검증 FIX 335/match훼손 10=33:1(공식 대기)** → 누계 ~47.2%.
   - **★"이게 최선?" 재반박 라운드(2026-07-22) — 잔여 12,306 4방향 전수 기각(재론 금지)**: ①행밀림 교정(ext=옆행 join기대값 증거→내 join으로 교체) FIX 334/BREAK 719 — join 자체가 충돌행에선 불신 ②밴드 whitelist 스캔(빈칸 4,111, 250장 replay) FIX 22/BREAK 46=31% — 행 텍스트의 wl런이 옆행/딴코드 ③itemCode칸 교차복사(428) FIX 35/BREAK 48 ④trust-join-over-valid 671/208=3.2:1 미달 ⑤윈도우폴백 261/37은 P1과 중복. **잔여 정체 = ~7,200 "깨끗한 형식인데 틀림"=master itemCode 오매칭의 하류(join 오염)+GT 코드체계 차이, ~3,200 recognition.** → **다음 언락=insuranceCode 룰이 아니라 itemCode 매칭률(현 60.6%) 개선**=마스터 랭킹 트랙([[project_master_match_baseline]] "+20 최대레버")이 오르면 join이 공짜로 따라 오름. ~47%=insuranceCode 룰 천장 확정.
5. **★itemCode 매칭 랭킹 트랙 착수(2026-07-22)**: itemCode defect=98.5% ocr_absent(문서에 코드 미인쇄=매칭이 만드는 값이라 drop-classify 무의미). **라이브 랭킹갭(3,000표본): GT가 top30 안 93.2%**, mismatch 860 분해=sim동률 tiebreak 패배 292(형제SKU)+top2-3 285+4-30 149+커버리지밖 134 → 랭킹-회수 풀 ~24%(~9,200셀, 상한 ~85%).
   - **✅ 1탄 "비마스터 itemCode 덮기" 구현·검증(2026-07-22, 미커밋)**: mismatch의 절반(4,588)=문서 인쇄 공급사코드('GMT26513')가 itemCode 칸 선점→master fill(빈칸만) 차단이 범인. `fill_master_match` 게이트 수정=비마스터 코드(`not in matcher._cd2i`)는 빈칸 취급(매칭 시도+덮기), 마스터코드는 불가침. **전수검증: itemCode FIX 3,515/BREAK 0**(인쇄코드==GT 행 전무=무위험)**+연쇄 insuranceCode FIX 657/BREAK 37(17.8:1)**(마스터코드化→join 자동발화). study 원천 회귀불가(itemCode/master/insurance 채점 밖). 예상: itemCode 60.6→~69.9%, insuranceCode ~48.5%(+P1=~50%). 배선 변경 없음(함수 내부만).
   - **잔여 랭킹 레버(귀속 실측, mismatch 6,000 중)**: lost_by_sim 1,165(이름 garble→딴 품목이 더 유사=클리닝/가격게이트) > lost_by_price 729(가격 tiebreak 오픽=ibc/spec 보강) > lost_by_dose 380(dose점수 — ★spec B2로 spec칸이 채워져 dose 입력 풍부해짐=재측정 가치) > tail/len 소형. gt_absent_top30 867=커버리지 한계.
   - **★공식 확정(2026-07-22 3차 run)**: itemCode 60.6→**69.8%**(+9.2pp), insuranceCode→**48.0%**(P1+연쇄), thin 전체셀 53.15→**54.29%**, spurious 0, study 957 불변, master cross −3(junk코드 행이 main-fill 경로로 이동하며 rawtext-fill과 픽 갈린 3행=노이즈, 체계 회귀 아님).
   - **세션 누적(2026-07-21~22, 전부 미커밋)**: thin 전체셀 **49.0→54.29%(+5.3pp)**. 파일: extractors/invoice_statement_free.py(spec B2)·extractors/master_match.py(보험 M1/M2/P1+비마스터덮기)·main.py(합류점 2곳)·eval/replay_compare.py(미러 2곳)·eval/normalize.py(norm_spec). 다음=잔여 랭킹 레버(lost_by_price 729→ibc/spec보강, lost_by_dose 380→spec B2 후 재측정) or 다음 컬럼(manufacturingNo 17k).
   - **커밋 대기(git은 사용자)**: spec 변경 4파일 = extractors/invoice_statement_free.py·main.py·eval/replay_compare.py(B)·eval/normalize.py(A). ⚠️ 작업트리에 사용자의 별개 변경 `eval/contract.py`(invoice_replay 9k 테스트셋 추가) 동반 — 내 것 아님, 커밋 분리 판단은 사용자.
   - **다음 액션=사용자 표준루프**: `replay_compare.py --testset invoice_{study,thin}` → `parser_drop_classify.py --testset ... --compare-dir replay_compare` 로 공식 spec셀·spurious·study회귀·master net 확정 후 커밋(git은 사용자).
   - 합계 3형제 완료: RA'/RB' + **RC' 밴드스캔**(0of3 앵커가드 143:1). 총액59.8·세액60.3·공급59.6%(+6~10pp). 잔여=recognition+모호. G1 no-anchor ~270셀만 옵션.

방법론 전제: [[project-war-gt-numeric-reliability]] 신뢰-렌즈(회색 분모 제외·산술앵커만) + 측정→드라이런→가드→전수검증→표준루프 공식확정 루프 유지. FT는 사용자 보류 상태.
